# Техническая спецификация Symancy (MVP)

**Версия:** 0.1  
**Дата:** 2025-09-16  
**Ответственные:** Product Owner, CTO, Tech Lead

## 1. Цели и критерии успеха

- **Бизнес-цели:** подтвердить спрос на психологический анализ кофейной гущи через Telegram, достичь 1 000 активных пользователей и конверсии в платящих не ниже 7% к концу месяца после публичного запуска, выйти на выручку 1,5 млн ₽ за первый квартал после релиза.
- **Продуктовые цели:** предоставить стабильный и быстрый AI-анализ (<= 30 секунд на ответ), обеспечить NPS ≥ 35, добиться удержания D30 ≥ 25%.
- **Технические цели:** запустить надежную инфраструктуру с доступностью бота ≥ 99%, обеспечить пригодность к расширению (Mini App, дополнительные мессенджеры) без полной переработки архитектуры.

## 2. Объем работ

### 2.1. В Scope (MVP фаза)
- Telegram Bot с основным сценариями (регистрация, загрузка фото, текстовый ввод, получение анализа, история последних 20 сессий).
- Интеграция с AI-провайдером (OpenRouter Vision или эквивалент) для анализа изображений и генерации текста.
- Бэкенд с PostgreSQL для хранения пользователей, сессий, подписок; Redis для лимитов и очередей задач.
- Поддержка тарифов Free, Trial, Basic, Advanced (без Premium) с лимитами по количеству фото и объему ответов.
- Оплата через Telegram Stars и СБП (через Тинькофф Кассу) с автоматическим переключением тарифов.
- Минимальный административный интерфейс (или CLI/Notion-процедура) для просмотра пользователей и ручного управления подписками.
- Система уведомлений: триггеры об успешной оплате, превышении лимитов, завершении анализа.
- Мониторинг и логирование (Sentry / аналог, базовые дашборды).
- Базовая аналитика: трекинг MAU, конверсии, платящие, удержание (через Amplitude/Metabase).

### 2.2. Вне Scope (последующие фазы)
- RAG, персонализация на истории и векторные БД.
- Интеграции с WhatsApp, VK, WeChat, Instagram.
- Премиум AI-модели (Claude/GPT-4) и тариф Premium.
- Дневник настроения, социальные функции, геймификация, реферальная программа.
- Автоматический A/B тестинг, геймификация, AR-функции.
- Голосовой бот, email-рассылки, push-уведомления вне Telegram.

### 2.3. Исходное состояние (Pre-MVP)
- Уже существует автоматизированный сценарий, обрабатывающий фото и текстовые сообщения в Telegram и возвращающий интерпретации (см. `docs/coffee-reader-pre-mvp.md`).
- Реализованы: скачивание и ресайз изображений, первичный визуальный анализ через vision-модель, психологическая интерпретация (агент «Арина»), постобработка HTML-ответов и их доставка частями.
- Для диалоговой последовательности используется PostgreSQL-память на уровне workflow (окно 20 сообщений), есть интеграция с двумя LLM через OpenRouter.
- В рамках MVP требуется перенести эту логику в промышленную архитектуру: выделить сервисы, обеспечить хранение данных, добавить биллинг, мониторинг и управление лимитами.

## 3. Пользователи и сценарии

### 3.1. Персоны
- **Анна, 32:** основная аудитория. Использует Telegram, интересуется психологией, ищет быстрые интерпретации и советы.
- **Мария, 27:** early adopter, готова делиться результатами с друзьями, ожидает современный UX и удобные оплаты.
- **Ольга, 40:** готова платить за глубокий анализ и стабильность, ценит хранение истории и рекомендации.
- **Поддержка/оператор:** сотрудники, обслуживающие вопросы пользователей и управление подписками.

### 3.2. Ключевые пользовательские сценарии
1. **Регистрация и onboarding**: пользователь запускает бота, получает приветствие, соглашается с условиями, указывает имя и цель (необязательно).
2. **Загрузка фото и получение анализа**: пользователь отправляет фото чашки или вводит текст символов, система анализирует и возвращает структурированный ответ.
3. **Просмотр истории**: пользователь запрашивает последние интерпретации и получает список.
4. **Оформление подписки**: пользователь выбирает тариф в боте или Mini App, оплачивает, получает подтверждение и обновление лимитов.
5. **Управление подпиской**: проверка статуса, смена тарифа, отмена автопродления, просмотр оставшихся лимитов.
6. **Получение уведомлений**: уведомление о готовности анализа, превышении лимитов, окончании Trial.

## 4. Функциональные требования

### 4.1. Клиентские интерфейсы
- **Telegram Bot**: поддержка кнопок (Reply/Inline), меню команд, состояния диалога, текстовые и медиасообщения.
- **WebApp (Mini App) v1**: авторизация через Telegram, отображение профиля (текущий тариф, лимиты), история до 20 записей, экран тарифов и оплата (перенаправление на webview/платежку).

### 4.2. Авторизация и учетные записи
- Пользователь идентифицируется по Telegram ID.
- Дополнительные поля: имя, часовой пояс (по возможности), сегмент (Free/Trial/Basic/Advanced), согласие с политикой.
- Сохранение статуса согласий (Terms of Use, Privacy) с датой.

### 4.3. Процесс анализа
- Прием фото (минимум 1080px по длинной стороне), проверка формата (JPEG/PNG ≤ 15 МБ, отсутствие NSFW/непрофильных изображений через простую модель/heuristics).
- Очередь обработки: фото помещается в очередь, в течение ≤ 10 секунд пользователь получает ответ «анализ начался».
- AI обработка: отправка изображения + контекст (история пользователя) в модель, генерация структурированного ответа (см. 4.4).
- Ограничение количества запросов в сутки и в месяц согласно тарифу (подсчет в Redis + запись в PostgreSQL).
- Обработка текстового ввода символов (fallback): отдельный промпт.

### 4.4. Формат ответа
- Ответ состоит из блоков: 1) «Главный инсайт», 2) «Эмоциональное состояние», 3) «Рекомендации» (3 шт.), 4) «Символы, которые я увидел» (список), 5) CTA (например, «Поделитесь с другом»).
- Ответ хранится в базе в текстовом виде + JSON-структура для аналитики.
- Для тарифов Basic/Advanced добавить короткую динамику по сравнению с предыдущими анализами (если есть >= 3 записи).
- Базовые промпты и правила форматирования из pre-MVP (vision-подсказка и системные инструкции «Арины») переносятся в репозиторий как versioned артефакты с возможностью редактирования через конфигурации.

### 4.5. История и контекст
- Хранение до 12 месяцев данных для всех тарифов (для Free показывать 7 последних записей, остальные скрытые).
- Возможность пользователю запросить «Историю» (последние 7 для Free, 90 дней для Basic, 365 дней для Advanced).
- Контекст в AI промптах: последние 3 анализа + краткая сводка профиля пользователя.

### 4.6. Подписки и биллинг
- Поддержка тарифов: Free, Trial (7 дней), Basic (299₽), Advanced (599₽).
- Логика Trial: один раз на пользователя, автоматический переход в Free при отсутствии оплаты, уведомление за 1 день до окончания.
- Подключение Telegram Stars:
  - Проверка webhook уведомлений, защита от повторов.
  - Обновление тарифа после подтверждения оплаты.
- Подключение СБП (Тинькофф Касса):
  - Создание платежной ссылки, отслеживание статусов (успех/отмена/возврат).
  - Recurring платежи через токены банка (на MVP, если доступно).
- Хранение транзакций, статусов, попыток биллинга.
- Информирование о списаниях и ошибках, механика повторных попыток.

### 4.7. Управление лимитами
- Настройка дневных и месячных лимитов по числу фото и текстовых запросов.
- Уведомление при достижении 80% лимита и при блокировке.
- Административная возможность вручную сбрасывать/увеличивать лимиты.

### 4.8. Администрирование
- Доступ через защищенный интерфейс (минимально: административная панель или CLI) для:
  - Поиска пользователя по Telegram ID/имени.
  - Просмотра истории оплат и подписок.
  - Ручного изменения тарифа, продления Trial, добавления бонусных лимитов.
  - Просмотра логов ошибок.
- Аудит действий администраторов.

### 4.9. Аналитика и отчётность
- События: регистрация, загрузка фото, успешный анализ, ошибка анализа, успешная/неуспешная оплата, переход между тарифами.
- Показатели: DAU/MAU, конверсия Free→Trial→Paid, среднее время ответа, churn, LTV/CAC (интеграция с внешними отчетами).
- Еженедельные отчеты о ключевых метриках (автоматическая выгрузка или ручной отчет).

### 4.10. Пользовательская поддержка
- Команда поддержки получает уведомления о критических ошибках и может инициировать outbound сообщение из бота.
- Встроенная команда «Помощь» с FAQ и контактами.

## 5. Нефункциональные требования

- **Производительность:** время реакции бота ≤ 3 сек на стандартные сообщения, полный цикл анализа ≤ 30 сек. Очередь должна масштабироваться до 500 анализов/час.
- **Доступность:** целевой SLA 99%; автоматическое восстановление при падении AI-провайдера (повтор запроса, fallback модель).
- **Безопасность:** шифрование данных в покое (PostgreSQL TDE или на уровне сервисов), TLS для всех внешних запросов, хранение токенов в защищённом Vault. Соответствие 152-ФЗ, наличие политики конфиденциальности.
- **Скалируемость:** сервисы контейнеризованы, поддерживают горизонтальное масштабирование (Docker + Kubernetes/другой оркестратор).
- **Наблюдаемость:** централизованные логи, метрики (Prometheus/Grafana), алерты по SLA и ошибкам.
- **Соответствие закону:** обработка персональных данных только на серверах в РФ или в соответствии с требованиями регуляторов; хранение consent.
- **Локализация:** интерфейс бота на русском, архитектура допускает добавление других языков (через конфигурацию).
- **UX/UI:** соблюсти гайд Telegram по ботам и WebApp, обеспечить доступность (контраст, крупные кнопки).

## 6. Архитектура и стек

- **Клиент:** Telegram Bot API, Telegram WebApp (React/Vue + TypeScript).
- **Бэкенд:** Node.js/TypeScript (Nest.js или Fastify) либо Python (FastAPI) — решение принимается CTO, критерия: экосистема, скорость разработки, наличие опыта.
- **Инфраструктура:** Docker контейнеры, Kubernetes (или Yandex Cloud Serverless Containers), PostgreSQL 15, Redis, S3-совместимое хранилище для изображений (Yandex Object Storage/MinIO).
- **AI-провайдер:** OpenRouter Vision API (fallback: локальная модель или другой провайдер). Подготовка промптов и шаблонов в конфигурации.
- **Интеграции:** Telegram Bot API, Telegram Payments, Тинькофф Касса API, Sentry/LogRocket, Amplitude/Metabase.
- **CI/CD:** GitHub Actions/GitLab CI, автоматические тесты и деплой на staging → production.

### 6.1. Логическая схема
1. Пользователь отправляет фото → Телеграм пересылает на вебхук бота.
2. Бот валидирует запрос, сохраняет метаданные в PostgreSQL, кладёт задачу в очередь (Redis/worker).
3. Worker загружает фото в S3, вызывает AI API, получает ответ, нормализует структуру, сохраняет в БД.
4. Бот отправляет пользователю промежуточный ответ («обработка началась») и итоговый результат.
5. Оплата: пользователь вызывает команду тарифа → Mini App → создание платежа → webhook → обновление статуса подписки.

### 6.2. Наследуемые компоненты из pre-MVP
- Набор промптов для vision-анализа и психологической интерпретации («Арина») — требуется вынести в конфигурацию и обеспечить контроль версий.
- Логика chunking/очистки HTML перед отправкой в Telegram — перенос кода в сервис отправки сообщений с покрытием тестами.
- Механика показа `typing` и задержек между частями ответа — оформить как часть пользовательского сервиса уведомлений.
- PostgreSQL-память диалога — перепроектировать как модуль хранения истории в основной БД, сохранив окно контекста и настройку глубины.

## 7. Управление данными и AI

- **Хранение изображений:** в S3-бакете с приватным доступом; URL ограничены по времени.
- **Хранение текстов:** PostgreSQL (таблицы `users`, `sessions`, `analyzes`, `subscriptions`, `transactions`).
- **Очистка данных:** политика хранения 12 месяцев, после истечения срока — анонимизация или удаление.
- **AI промпты:** хранятся в конфигурации, версия контролируется в репозитории; необходим split для фото и текстового ввода.
- **Качество AI:** еженедельный ручной контроль 20 случайных сессий, метрика успешных/неудачных анализов ≥ 95%.
- **Фидбек:** пользователь может оценить ответ (👍/👎); сохранять для дообучения.

## 8. Тестирование и качество

- **Типы тестов:** unit-тесты для бизнес-логики (лимиты, подписки), интеграционные тесты для платежей и AI, end-to-end тесты (Telegram бот, Mini App).
- **Тестовые окружения:** staging с тестовыми токенами Telegram, демо-аккаунтом Тинькофф Кассы, sandbox AI.
- **Критерии приемки:** чек-листы по ключевым сценариям (регистрация, анализ, оплата). Нагрузочный тест — 200 параллельных анализов.
- **Обработка ошибок:** повтор запросов при таймаутах (до 3 попыток), логирование причин отказа, пользовательские сообщения о проблемах.

## 9. Поставки и план релизов

### 9.1. Этапы
1. **Неделя 1-2:** уточнение требований, дизайн архитектуры, подготовка инфраструктуры.
2. **Неделя 3-6:** разработка Core (бот, анализ, БД, лимиты).
3. **Неделя 7-8:** интеграция платежей, Mini App v1, аналитика и мониторинг.
4. **Неделя 9:** QA, нагрузочные тесты, фиксы.
5. **Неделя 10:** soft launch (до 100 пользователей), сбор обратной связи.
6. **Неделя 11-12:** оптимизация, подготовка к публичному запуску.

### 9.2. Релизные артефакты
- Обновляемый backlog в Jira/YouTrack.
- Техническая документация (диаграммы, конфигурации).
- План incident response с контактами ответственных.

## 10. Риски и открытые вопросы

- **Зависимость от AI-провайдера:** потребовать SLA или готовить fallback модель.
- **Платежи СБП:** риск задержек интеграции с Тинькофф → предусмотреть резервный сценарий (пока только Telegram Stars).
- **Качество фото:** пользователи могут отправлять нерелевантные изображения → требуется обучение и валидация.
- **Юридические ограничения:** необходимо подтвердить возможность обработки персональных данных и психологической информации (консультация с юристом).
- **Нагрузка при виральности:** предусмотреть масштабирование очередей, лимиты на пользователя.
- **Открытые вопросы:**
  - Уточнить юридическую модель (ИП/ООО) для подключения платежей.
  - Выбор конкретной AI модели и бюджет на инференс.
  - Подтверждение дизайна Mini App (нужен UI-kit).

---

Документ предназначен для команды разработки и смежных функций. Все изменения и дополнения оформляются через pull request с ревью CTO/PO.
